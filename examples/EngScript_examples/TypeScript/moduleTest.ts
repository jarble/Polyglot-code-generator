engScript_add(engScript_a : number, engScript_b : number): number{
    return (engScript_a + engScript_b);
}
engScript_multiply(engScript_a : number, engScript_b : number): number{
    return (engScript_a * engScript_b);
}

