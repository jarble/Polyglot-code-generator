sub engScript_add{
$engScript_a = shift;
$engScript_b = shift;
    return ($engScript_a + $engScript_b);
}
sub engScript_multiply{
$engScript_a = shift;
$engScript_b = shift;
    return ($engScript_a * $engScript_b);
}

